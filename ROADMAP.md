# 项目里程碑与后续路径 (ROADMAP)

本文档记录了当前系统的技术瓶颈、待优化项以及通往生产环境的路线图。

## 1. 当前核心瓶颈：全局性总结问题
- **现象**：当提问“这个知识库讲了什么？”或“总结一下文档”时，系统回答“无法回答”。
- **根因**：
    - **检索偏置**：目前的 Top-K 向量检索只擅长寻找“细节片段”，无法捕捉文档全貌。
    - **阈值截断**：Rerank 分数过低导致检索结果为空，触发了系统的安全降级逻辑。
- **优化方案 (Next Steps)**：
    - [ ] **意图识别 (Intent Routing)**：在检索前识别用户是否在寻求“总结”。
    - [ ] **增强型召回**：对于总结类意图，强制召回文档的前 N 个分片（通常包含 H1 和简介）或目录结构（heading_path）。
    - [ ] **知识库摘要**：在 Ingestion 阶段增加一步 LLM 摘要提取，并持久化到 `knowledge_bases` 表。

## 2. 后端技术债务与增强
- [ ] **混合检索优化 (RRF)**：目前是简单的并集去重，建议引入 Reciprocal Rank Fusion 算法进行更科学的权重融合。
- [ ] **流式性能优化**：目前的首字延迟（TTFT）仍有压缩空间，可考虑在检索阶段增加更细粒度的流式反馈。
- [ ] **可观测性集成**：集成 LangSmith 或部署本地的 Arize Phoenix，用于监控 RAG 的召回率和幻觉指标。

## 3. 生产部署路线图
- [ ] **API 限流 (Rate Limiting)**：增加基于 IP 或 UserID 的请求频率限制，防止 OpenAI 账单异常。
- [ ] **一键部署 (CI/CD)**：完善 `docker-compose.prod.yml`，配置 GitHub Actions 实现自动化部署到 Railway/Supabase。
- [ ] **评估集建设 (Eval)**：利用 `rag_eval_service.py` 运行标准测试集，量化每次代码变更对准确率的影响。
