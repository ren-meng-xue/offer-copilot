# AI 驱动开发 (AIDD) 面试策略指南

本文档旨在帮助你将“使用 AI 开发项目”这一事实转化为面试中的核心竞争优势。

## 1. 核心定位：AI 原生架构师 (AI-Native Architect)
不要定位为“代码编写者”，而要定位为“Agent 工作流的编排者”。

### 核心卖点
- **极致的交付效率**：通过编排 AI Agent，在极短时间内交付了具备 Hybrid Search + Rerank 的闭环系统。
- **架构管控能力**：通过编写结构化的 Spec 和架构约束（CLAUDE.md），确保 AI 生成的代码符合工业级规范（分层架构、DRY原则）。
- **工具链掌控力**：熟练驾驭 Codex、gstack、superpowers 等 Agent 协作工具，代表了下一代软件开发的范式。

## 2. 关键术语与话术 (Buzzwords & Narratives)

### Spec-Driven Development (SDD)
- **定义**：通过定义严密的“技术规范文件”作为 Agent 执行的输入，而非模糊的对话。
- **话术**：*“我采用的是规范驱动开发。Spec 对我来说不是需求文档，而是我与 Agent 之间的‘架构契约’。这保证了在复杂项目中，代码生成的一致性和可维护性。”*

### 反馈闭环 (Feedback Loop)
- **话术**：*“当 AI 遇到检索瓶颈时，我没有手动修代码，而是通过分析 RAG Trace 数据，发现是分片算法在 Markdown 表格上的缺陷。随后我更新了切片服务的规范约束，引导 Agent 重新实现了基于语义块的切片算法。”*

## 3. 常见 Q&A 应对

**Q：既然代码是 AI 写的，你的价值在哪里？**
**A**：*“AI 是高效的执行者，而我是系统的架构师和质量守门员。RAG 的混合检索方案选型、Citations 的正则表达式校验逻辑、以及异步任务的状态机设计，都是由我进行决策和设计的。我负责‘怎么做’和‘为什么这么做’，AI 负责‘把它写出来’。”*

**Q：你怎么证明你对这些生成的代码有深度掌控？**
**A**：*“每一行核心逻辑（如 `qa_service.py` 中的 Rerank 过滤逻辑）都经过我的代码审计和 Spec 校验。我不仅理解代码，更理解这些技术选型背后的 Trade-offs，比如为什么选 pgvector 而非外部向量库，以及 Cohere Rerank 对延迟的影响。”*
