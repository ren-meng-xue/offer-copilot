# Dev RAG - Gemini 主控规范

> [!IMPORTANT]
> **本项目以项目根目录下的 `CLAUDE.md` 为唯一权威的主控规范。**
> Gemini 在开始会话、执行任何操作或回答任何问题前，**必须首先主动且完整地阅读一遍 `CLAUDE.md`**（使用文件查看工具），并以其中规定的全局约束、Skill 路由表和技术栈作为最高行动标准。

本文件是项目内 Gemini 的入口规范。为了保持规范的单一可信源（Single Source of Truth），本文件不再维护重复的细节，所有具体规范请直接参考项目根目录下的 [CLAUDE.md](file:///Users/xuebao/learn/AI%E9%A1%B9%E7%9B%AE/offer-copilot/CLAUDE.md)。

## 全局核心约束（始终生效，任何情况不可跳过）

1. **必须首先阅读 `CLAUDE.md`**：开始会话后，第一步必须首先完整阅读一遍项目根目录下的 `CLAUDE.md` 文件，并严格遵守。
2. 始终使用**简体中文**回复。
3. 所有配置走环境变量，**禁止硬编码** Key / Secret。
4. commit / push 前必须等待用户回复「**1**」，否则不执行。
5. LLM 调用只在 `services/` 层，`api/` 层禁止直接调用。
6. 实时状态用 SSE + Redis Pub/Sub，**禁止轮询**。
7. 数据库变更必须走 Alembic，禁止直接改表结构。
8. 删除任何文件、目录、数据、分支或远程资源前，必须先说明删除目标与影响范围，并等待用户明确允许后再删除。
