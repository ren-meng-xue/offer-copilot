# 项目说明

## 语言要求
- **始终**使用**中文**（简体中文）与用户交流。
- 所有计划、总结和聊天回复必须使用中文。
- 代码注释和技术文档应遵循现有项目规范（如无特殊说明，代码级文档建议使用英文）。

## 运行约定
- **始终**使用**中文**（简体中文）与用户交流。
- **测试优先**：每次代码更改后，必须先运行相关构建验证（如 `pnpm build`）或测试脚本。
- **质量闭环**：只有在验证无误后，才可主动进行 `commit` 和 `push`。
- **变更记录**：
  - 在根目录 `changelogs/` 文件夹下维护变更记录。
  - 文件名格式为 `YYYY-MM-DD.md`（例如 `2026-05-12.md`）。
  - 内容格式：使用 Todo List 形式并配合**图标（Emoji）**记录。
    - 例如：`- [x] 🐛 修复了...` 或 `- [x] 🚀 实现了...`。
  - **确认机制**：在更新当日 Changelog 后，**必须**向用户展示内容并等待明确回复（如“同意”、“1”或“ok”）。
  - 只有在获得用户许可后，方可执行最终的 `commit` 和 `push`。

## 架构与约定
- **事件驱动状态**：使用 SSE（服务端事件）和 Redis Pub/Sub 实现实时状态更新，禁止使用轮询。
- **上下文感知嵌入**：生成嵌入前，必须在内容前加上 `heading_path`。
- **摘要生成**：在文档入库时生成并存储全局摘要。
- **文件上传**：本地文件入库需支持 multipart/form-data。

## 实现细节
- 后端：FastAPI、Celery、Redis、SQLAlchemy（异步）。
- 前端：React（Next.js）、TypeScript、Tailwind CSS。
