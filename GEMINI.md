# 项目说明

## 语言要求
- **始终**使用**中文**（简体中文）与用户交流。
- 所有计划、总结和聊天回复必须使用中文。
- 代码注释和技术文档应遵循现有项目规范（如无特殊说明，代码级文档建议使用英文）。

## 架构与约定
- **事件驱动状态**：使用 SSE（服务端事件）和 Redis Pub/Sub 实现实时状态更新，禁止使用轮询。
- **上下文感知嵌入**：生成嵌入前，必须在内容前加上 `heading_path`。
- **摘要生成**：在文档入库时生成并存储全局摘要。
- **文件上传**：本地文件入库需支持 multipart/form-data。

## 实现细节
- 后端：FastAPI、Celery、Redis、SQLAlchemy（异步）。
- 前端：React（Next.js）、TypeScript、Tailwind CSS。
