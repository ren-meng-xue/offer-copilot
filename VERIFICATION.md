# 手动验收清单

## 环境检查

- [x] 前端 dev server 已运行（`http://localhost:3000`）
- [x] 后端服务已运行（API 可访问）
- [ ] 已登录（或可以正常登录）

## /chat 验收

1. **进入草稿态**
   - [ ] 访问 `http://localhost:3000/chat`
   - [ ] 确认能看到知识库下拉选择框
   - [ ] 确认至少有一个 `done` 状态的知识库

2. **发送首问**
   - [ ] 选择一个知识库
   - [ ] 输入问题并发送
   - [ ] **关键验证**：点击发送后，输入框内容是否立即被清空（不等待回答生成完）
   - [ ] 验证看到乐观消息（user message + assistant draft）立即出现在消息区

3. **跳转到会话页**
   - [ ] **关键验证**：页面跳转到 `/chat/[id]` 时：
     - 是否没有出现明显的空白闪烁（"正在加载会话..."）
     - 当前知识库信息是否仍然可见（顶部应有"当前知识库：XXX"）
   - 是否还能看到刚发送的乐观消息

4. **继续提问**
   - [ ] 在已创建的会话中继续提问
   - [ ] 验证回答能正常生成并显示

## /knowledge 验收

1. **进入知识库页**
   - [ ] 访问 `http://localhost:3000/knowledge`
   - [ ] 确认能看到知识库列表

2. **搜索功能**
   - [ ] **关键验证**：确认页面顶部有"搜索知识库"输入框
   - [ ] 输入关键词（如"FastAPI"）
   - [ ] 验证列表能即时过滤，只显示匹配项

3. **删除功能**
   - [ ] 找到 `done` 或 `failed` 状态的知识库
   - [ ] **关键验证**：点击"删除"按钮
   - [ ] 验证该项从列表中消失
   - [ ] 找到 `pending` 或 `processing` 状态的知识库
   - [ ] **关键验证**：删除按钮应为禁用状态
   - [ ] 验证下方显示"索引中暂不可删除"提示

4. **导入按钮样式**
   - [ ] **关键验证**：确认"导入文档"按钮比之前更明显：
     - 尺寸更大
     - 主色调更明显
     - 视觉层次更清晰

## 回归测试验证

如果需要重新运行单元测试：

```bash
cd frontend
npm test src/features/chat/components/chat-page.test.tsx src/features/knowledge/components/knowledge-page.test.tsx src/services/knowledge.test.ts
```

应该看到 18 个测试全部通过。
